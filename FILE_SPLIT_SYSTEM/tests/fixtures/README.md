# Validation fixtures

Real customer originals and outputs are deliberately not committed. Controlled Illustrator validation must
be run locally for OBC-241A, OPF-504A, OMO-522A, and OBC-292A, recording only redacted pass/fail metadata
in `executors/manifest.json`.
