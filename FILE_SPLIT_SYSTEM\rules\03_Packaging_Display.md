# 03 包装效果图处理规则

本文件是 `PACKAGING_DISPLAY` 分支的权威规则。目标是把包装展开稿的有效包装面拆分并平面组合为一张 PNG，不进行三维渲染。

## 1. 输出

- 文件名：`{model}_包装效果图.png`，例如 `OBC-292A_包装效果图.png`。
- 所有保留面一起导出为一张 PNG，300 ppi，包装面以外区域为白色。
- 不覆盖原稿，不把各面分别导出为多张图片。

## 2. 主稿和辅助内容

识别正式包装主稿与其结构。主稿以外的 UV 副稿、独立刀模副稿、工艺说明、尺寸标注、辅助色块和其他生产辅助内容应删除，但不能仅因对象位于固定方向或远离主稿就删除。

删除任何主稿刀模前，先记录刀模定义的包装面边界、尺寸、相邻关系和方向。边界不可靠时停止编辑并标记 `needs_review`。

## 3. 保留面

保留含信息内容的包装面。信息包括文字、Logo、产品图、条码、二维码、认证图标及有意义的图案。删除无信息的折翼、粘口和辅助结构。

单纯大面积颜色不是“有信息内容”的充分依据；需结合其是否构成正式可见包装面判断。跨面对象按真实面边界分配或裁切，保证图案连续性，不得丢失或重复保留。

每面记录：原边界与尺寸、内容对象、背景构成、方向、相邻面、目标位置及是否存在跨面对象。

## 4. 底板与群组

在每个保留面内容的下方建立与该面实际尺寸完全一致的矩形底板，底板必须有描边，并与该面内容组成独立群组。

面背景必须保持原稿：白底仍为白底，绿色或其他底色仍保留原底色；不得用新底板覆盖已有渐变、纹理、图案或多色背景。绿色面不得改成白色。

用户尚未指定描边线宽。优先参考同任务批准案例的细边框外观，并在任务记录中写明实际采用值与依据；无可靠依据时将配置值保持 `null` 并请求确认，不能把推定值写成用户确认标准。

## 5. 刀模清理

面边界已记录、底板已建立且面群组已复核后，删除原刀模线、折线及无效结构。新建底板描边必须保留。以对象用途、图层、色样和几何关系综合判断，避免误删正文图形、装饰线或认证标志。

## 6. 排列

各面只允许平移；不得缩放、旋转、镜像或改变文字方向，包括原稿中倒置或侧向的内容。

尽量保持原展开图中可识别的相对排列关系，同时压缩删除折翼、粘口和辅助结构产生的空区；不强制所有面排成一行。

横向与纵向相邻面的边缘净间距统一为 `10 mm`，从实际外边缘计算，不按中心点计算。排布后任何面、描边或有效内容均不得重叠。案例旧间距不覆盖本规则。

## 7. 画板和导出

画板须完整包含全部面及描边，不能裁切。用户尚未指定画布外边距；采用合理且一致的临时值时必须记录，并保留为可调参数，不得宣称为已确认标准。

以 300 ppi 导出单张白背景 PNG。白背景只属于包装面外区域，不覆盖任何包装面本身的颜色。导出后用画板物理尺寸核算期望像素尺寸：`像素 = 毫米 / 25.4 × 300`（按导出舍入规则允许合理整数误差），不能只因导出成功就判定分辨率正确。

若像素尺寸、画板尺寸或软件限制超限，停止导出并报告；不得自行缩小内容、降低 ppi 或拆成多张图片。

